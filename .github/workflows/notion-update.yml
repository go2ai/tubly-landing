name: Update Notion Table
on:
  push:
    branches:
      - master
jobs:
  update-notion:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4.1.1

    - name: Set up Node.js
      uses: actions/setup-node@v4.0.0
      with:
        node-version: '18'

    - name: Install dependencies
      run: npm install

    - name: Run Notion update script
      run: run: node notion-integration.js "${{ github.event.head_commit.url }}" "${{ github.event.head_commit.timestamp }}" "${{ github.event.pull_request.html_url }}" "${{ github.repository }}" "${{ github.actor }}"
      env:
        NOTION_TOKEN: ${{ secrets.NOTION_INTEGRATION_TOKEN }}
